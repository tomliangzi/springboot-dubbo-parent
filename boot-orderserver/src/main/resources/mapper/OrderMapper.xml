<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jerry.bootorderserver.mapper.OrderMapper">

    <select id="findorderbyid" resultType="org.example.entity.Order" parameterType="java.lang.Integer">
        SELECT id ,address FROM myorder where id=#{id}
    </select>

</mapper>